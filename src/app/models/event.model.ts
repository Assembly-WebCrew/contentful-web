export class AsmEvent {
  defaultBackground: EventDefaultBackground;
  endDate: string;
  eventPlace: string;
  eventTitle: string;
  logo: any;
  name: string;
  startDate: string;
}

export class EventDefaultBackground {
  fields: {
    file: {
      url: string
    }
  };
}
