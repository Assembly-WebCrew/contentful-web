dist: trusty
language: node_js

node_js:
  - "8"

addons:
  chrome: stable

env:
  - CHROME_BIN=google-chrome-stable

cache: yarn

jobs:
  include:
    - script: yarn test -- --singleRun
    - script: yarn e2e
    - stage: build
      script: yarn build
    - stage: deploy
      script: echo "Deploying!"
